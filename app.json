{
    "scripts": {
       	"postdeploy": "sh heroku-post-deploy.sh"
    },
    "env": {
        "HEROKU_APP_NAME": {
	    "required": true
        }
    },
    
    "addons": ["heroku-postgresql:standard-0"]
}
